# 프로덕션(홈서버) 프로파일 — nginx /redmine-api/ → 127.0.0.1:8082/api/
server.port=8082

# PM2 작업 디렉토리 기준 ./data/redmine.db 에 SQLite 생성
spring.datasource.url=jdbc:sqlite:./data/redmine.db

spring.jpa.show-sql=false
